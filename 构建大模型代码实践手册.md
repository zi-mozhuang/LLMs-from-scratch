# 从零构建大语言模型：代码实践手册

> 基于官方代码库 [LLMs-from-scratch](https://github.com/rasbt/LLMs-from-scratch) 编写。按本手册顺序执行，即可完成从文本处理到指令微调的完整 GPT 构建流程。
> 所有代码均为官方实现（与 `pkg/llms_from_scratch/` 及各章 `01_main-chapter-code/` 一致），可直接复制运行。

## 全流程总览

| 步骤 | 对应章节 | 目标产物 | 仓库参考目录 |
|---|---|---|---|
| 1. 环境准备 | 本手册 §1 | 可运行的 Python 环境 | `setup/`、`requirements.txt` |
| 2. 文本处理管道 | ch02 | BPE 分词 + DataLoader | `ch02/01_main-chapter-code/ch02.ipynb` |
| 3. 注意力机制 | ch03 | 因果多头自注意力 | `ch03/01_main-chapter-code/ch03.ipynb` |
| 4. GPT 模型组装 | ch04 | 完整 GPTModel | `ch04/01_main-chapter-code/gpt.py` |
| 5. 预训练 | ch05 | 训练循环 + GPT-2 权重加载 | `ch05/01_main-chapter-code/gpt_train.py` |
| 6. 分类微调 | ch06 | 垃圾邮件分类器 | `ch06/01_main-chapter-code/ch06.ipynb` |
| 7. 指令微调 | ch07 | 指令跟随模型 | `ch07/01_main-chapter-code/gpt_instruction_finetuning.py` |

**核心思路**：用纯 PyTorch 复刻 GPT-2 small（124M）架构 —— 分词 → 嵌入 → 12 层 Transformer 块（因果多头注意力 + 前馈网络，均带 LayerNorm 和残差连接）→ 输出头预测下一个 token。训练目标是最小化交叉熵损失；之后加载官方 GPT-2 预训练权重，再做下游微调。

---

## §1 环境准备

**要求**：Python 3.10+；PyTorch ≥ 2.2.2（有 CUDA 自动用 GPU；无 GPU 可全程 CPU 运行，只是慢）；不依赖任何外部 LLM 库。

```bash
# 方式一：pip（在仓库根目录执行）
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# 方式二：uv（官方推荐，更快）
curl -LsSf https://astral.sh/uv/install.sh | sh
uv sync --dev
uv pip install -r requirements.txt

# （可选）将本仓库函数装成可导入的包
pip install -e .   # 之后可 from llms_from_scratch.ch04 import GPTModel
```

**依赖清单**（摘自 `requirements.txt`）：

| 库 | 版本 | 用途 |
|---|---|---|
| `torch` | ≥2.2.2 | 模型与训练 |
| `tiktoken` | ≥0.5.1 | GPT-2 BPE 分词器 |
| `jupyterlab` | ≥4.0 | 交互式运行（`.ipynb` 是主要形式） |
| `matplotlib` | ≥3.7.1 | 绘制训练曲线 |
| `tensorflow` | ≥2.18.0 | 读取官方 GPT-2 checkpoint（仅 §5.6 需要） |
| `tqdm` / `numpy` / `pandas` / `psutil` | 见 requirements | 下载进度、数据处理、评分 |

**验证环境**：

```python
import torch
print(torch.__version__, torch.cuda.is_available())
```

**约定**：以下代码建议放在一个工作目录中，按章节拆成多个 `.py` / notebook 逐步执行；后文引用前面定义的类时默认已导入。

---

## §2 步骤一：文本处理管道（ch02）

**目标**：把原始文本变成模型可吃的 token ID 批次：`文本 → BPE 分词 → token ID → 滑动窗口切片 → (输入, 目标) 批次`。

### 2.1 分词：使用 GPT-2 的 BPE 分词器

`tiktoken` 实现了 GPT-2 的字节对编码（BPE），词表大小 50257。

```python
import tiktoken

tokenizer = tiktoken.get_encoding("gpt2")

text = "Hello, do you like tea? <|endoftext|> In the sunlit terraces of the palace."
integers = tokenizer.encode(text, allowed_special={"<|endoftext|>"})
strings = tokenizer.decode(integers)
# integers 形如 [15496, 11, 466, 345, 588, 2428, 58, 220, 50256, 554, 262, ...]
# decode 后应还原出原文本
```

要点：
- `allowed_special={"<|endoftext|>"}` 让特殊结束符 `<|endoftext|>`（ID 50256）被编码而不是报错。
- BPE 不逐字符分词，常见词整体成 token，未知组合拆成子词；不存在未登录词。

### 2.2 滑动窗口切分：GPTDatasetV1

语言模型训练样本是「输入 = 原文，目标 = 原文右移一位」。用滑动窗口把长文本切成固定长度的重叠片段：

```python
import torch
from torch.utils.data import Dataset, DataLoader


class GPTDatasetV1(Dataset):
    def __init__(self, txt, tokenizer, max_length, stride):
        self.tokenizer = tokenizer
        self.input_ids = []
        self.target_ids = []

        # 对整段文本分词
        token_ids = tokenizer.encode(txt, allowed_special={"<|endoftext|>"})

        # 滑动窗口：切成若干长度为 max_length、步长为 stride 的重叠片段
        for i in range(0, len(token_ids) - max_length, stride):
            input_chunk = token_ids[i:i + max_length]
            target_chunk = token_ids[i + 1: i + max_length + 1]  # 右移一位
            self.input_ids.append(torch.tensor(input_chunk))
            self.target_ids.append(torch.tensor(target_chunk))

    def __len__(self):
        return len(self.input_ids)

    def __getitem__(self, idx):
        return self.input_ids[idx], self.target_ids[idx]
```

### 2.3 DataLoader 工厂函数

```python
def create_dataloader_v1(txt, batch_size=4, max_length=256,
                         stride=128, shuffle=True, drop_last=True, num_workers=0):
    tokenizer = tiktoken.get_encoding("gpt2")
    dataset = GPTDatasetV1(txt, tokenizer, max_length, stride)
    dataloader = DataLoader(
        dataset, batch_size=batch_size, shuffle=shuffle,
        drop_last=drop_last, num_workers=num_workers)
    return dataloader
```

参数说明：
- `max_length`：每个样本的 token 数，须 ≤ 模型上下文长度。
- `stride`：窗口滑动步长。`stride == max_length` 表示不重叠（预训练用）；`stride < max_length` 产生重叠样本（数据少时增加样本量）。
- `drop_last=True`：训练时丢弃不足一个 batch 的尾批，保持形状稳定。

### 2.4 验证

```python
with open("the-verdict.txt", "r", encoding="utf-8") as f:  # ch02/01_main-chapter-code/ 下自带该文件
    raw_text = f.read()

dataloader = create_dataloader_v1(
    raw_text, batch_size=8, max_length=4, stride=4, shuffle=False)
input_batch, target_batch = next(iter(dataloader))
print(input_batch.shape)   # torch.Size([8, 4])
print(target_batch.shape)  # torch.Size([8, 4])
# 且 target_batch 每个位置 == 该样本输入右移一位后的 token
```

✅ 完成标志：能拿到形状为 `(batch_size, max_length)` 的输入/目标张量，目标相对输入右移一位。

---

## §3 步骤二：注意力机制（ch03）

**目标**：实现因果（causal）多头自注意力 —— GPT 的核心组件。

> 演进路径：朴素自注意力 → 因果掩码（禁止看未来）→ 批处理 → 多头。下面直接给出可用的最终组件；`SelfAttention_v1/v2`（`llms_from_scratch.ch03`）仅用于理解原理，可跳过。

### 3.1 因果自注意力 CausalAttention

- Q/K/V 各由一个线性层投影得到；
- 注意力分数除以 √d 缩放后做 softmax；
- 上三角掩码把「未来位置」的分数置为 -∞，保证每个 token 只关注它之前的 token；
- Dropout 施加在注意力权重上。

```python
import torch
import torch.nn as nn


class CausalAttention(nn.Module):
    def __init__(self, d_in, d_out, context_length, dropout, qkv_bias=False):
        super().__init__()
        self.d_out = d_out
        self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)
        self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)
        self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)
        self.dropout = nn.Dropout(dropout)
        self.register_buffer(
            "mask",
            torch.triu(torch.ones(context_length, context_length), diagonal=1))

    def forward(self, x):
        b, num_tokens, d_in = x.shape
        keys = self.W_key(x)
        queries = self.W_query(x)
        values = self.W_value(x)

        attn_scores = queries @ keys.transpose(1, 2)
        attn_scores.masked_fill_(
            self.mask.bool()[:num_tokens, :num_tokens], -torch.inf)
        attn_weights = torch.softmax(
            attn_scores / keys.shape[-1]**0.5, dim=-1)
        attn_weights = self.dropout(attn_weights)

        context_vec = attn_weights @ values
        return context_vec
```

### 3.2 多头注意力 MultiHeadAttention（正式组件）

不采用「多个 CausalAttention 拼接」的低效写法，而是一次性投影后按头切分计算（等价但更快），最后经 `out_proj` 线性层合并各头：

```python
class MultiHeadAttention(nn.Module):
    def __init__(self, d_in, d_out, context_length, dropout, num_heads, qkv_bias=False):
        super().__init__()
        assert d_out % num_heads == 0, "d_out must be divisible by n_heads"

        self.d_out = d_out
        self.num_heads = num_heads
        self.head_dim = d_out // num_heads  # 每个头的投影维度

        self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)
        self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)
        self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)
        self.out_proj = nn.Linear(d_out, d_out)  # 合并各头输出
        self.dropout = nn.Dropout(dropout)
        self.register_buffer(
            "mask",
            torch.triu(torch.ones(context_length, context_length), diagonal=1))

    def forward(self, x):
        b, num_tokens, d_in = x.shape

        keys = self.W_key(x)       # (b, num_tokens, d_out)
        queries = self.W_query(x)
        values = self.W_value(x)

        # 增加 num_heads 维度，隐式拆成多头
        # (b, num_tokens, d_out) -> (b, num_tokens, num_heads, head_dim)
        keys = keys.view(b, num_tokens, self.num_heads, self.head_dim)
        values = values.view(b, num_tokens, self.num_heads, self.head_dim)
        queries = queries.view(b, num_tokens, self.num_heads, self.head_dim)

        # (b, num_tokens, num_heads, head_dim) -> (b, num_heads, num_tokens, head_dim)
        keys = keys.transpose(1, 2)
        queries = queries.transpose(1, 2)
        values = values.transpose(1, 2)

        # 每个头独立做缩放点积注意力
        attn_scores = queries @ keys.transpose(2, 3)
        mask_bool = self.mask.bool()[:num_tokens, :num_tokens]
        attn_scores.masked_fill_(mask_bool, -torch.inf)

        attn_weights = torch.softmax(attn_scores / keys.shape[-1]**0.5, dim=-1)
        attn_weights = self.dropout(attn_weights)

        # (b, num_heads, num_tokens, head_dim) -> (b, num_tokens, num_heads, head_dim)
        context_vec = (attn_weights @ values).transpose(1, 2)

        # 合并各头：num_heads * head_dim == d_out
        context_vec = context_vec.contiguous().view(b, num_tokens, self.d_out)
        context_vec = self.out_proj(context_vec)
        return context_vec
```

### 3.3 验证

```python
torch.manual_seed(123)
batch = torch.rand(2, 4, 16)  # (batch=2, tokens=4, d_in=16)
mha = MultiHeadAttention(d_in=16, d_out=32, context_length=8,
                          dropout=0.0, num_heads=2)
out = mha(batch)
print(out.shape)  # torch.Size([2, 4, 32])，输入形状只改变最后一维
```

✅ 完成标志：输出形状 `(b, num_tokens, d_out)`，且第 t 个位置的输出不依赖第 t 位之后的输入（可用截断输入验证）。

---

## §4 步骤三：GPT 模型组装（ch04）

**目标**：实现 GPT 的其余部件并组装成完整模型：
`token 嵌入 + 位置嵌入 → Dropout → N × [LayerNorm → 多头注意力 → Dropout → 残差 → LayerNorm → 前馈网络 → Dropout → 残差] → LayerNorm → 输出头`

### 4.1 层归一化 LayerNorm

对每个样本的每个特征向量做归一化（均值 0、方差 1），再用可学习的 γ（scale）和 β（shift）缩放平移：

```python
class LayerNorm(nn.Module):
    def __init__(self, emb_dim):
        super().__init__()
        self.eps = 1e-5
        self.scale = nn.Parameter(torch.ones(emb_dim))
        self.shift = nn.Parameter(torch.zeros(emb_dim))

    def forward(self, x):
        mean = x.mean(dim=-1, keepdim=True)
        var = x.var(dim=-1, keepdim=True, unbiased=False)
        norm_x = (x - mean) / torch.sqrt(var + self.eps)
        return self.scale * norm_x + self.shift
```

### 4.2 激活函数 GELU（GPT-2 使用的 tanh 近似版）

```python
class GELU(nn.Module):
    def __init__(self):
        super().__init__()

    def forward(self, x):
        return 0.5 * x * (1 + torch.tanh(
            torch.sqrt(torch.tensor(2.0 / torch.pi)) *
            (x + 0.044715 * torch.pow(x, 3))
        ))
```

### 4.3 前馈网络 FeedForward

两层线性 + 中间 GELU；隐藏层宽度是嵌入维度的 4 倍（768 → 3072 → 768）：

```python
class FeedForward(nn.Module):
    def __init__(self, cfg):
        super().__init__()
        self.layers = nn.Sequential(
            nn.Linear(cfg["emb_dim"], 4 * cfg["emb_dim"]),
            GELU(),
            nn.Linear(4 * cfg["emb_dim"], cfg["emb_dim"]),
        )

    def forward(self, x):
        return self.layers(x)
```

### 4.4 Transformer 块 TransformerBlock

Pre-Norm 结构：先归一化再过子层，配合残差连接与 Dropout：

```python
class TransformerBlock(nn.Module):
    def __init__(self, cfg):
        super().__init__()
        self.att = MultiHeadAttention(
            d_in=cfg["emb_dim"],
            d_out=cfg["emb_dim"],
            context_length=cfg["context_length"],
            num_heads=cfg["n_heads"],
            dropout=cfg["drop_rate"],
            qkv_bias=cfg["qkv_bias"])
        self.ff = FeedForward(cfg)
        self.norm1 = LayerNorm(cfg["emb_dim"])
        self.norm2 = LayerNorm(cfg["emb_dim"])
        self.drop_shortcut = nn.Dropout(cfg["drop_rate"])

    def forward(self, x):
        # 注意力子块 + 残差
        shortcut = x
        x = self.norm1(x)
        x = self.att(x)
        x = self.drop_shortcut(x)
        x = x + shortcut

        # 前馈子块 + 残差
        shortcut = x
        x = self.norm2(x)
        x = self.ff(x)
        x = self.drop_shortcut(x)
        x = x + shortcut

        return x
```

### 4.5 完整模型 GPTModel

```python
class GPTModel(nn.Module):
    def __init__(self, cfg):
        super().__init__()
        self.tok_emb = nn.Embedding(cfg["vocab_size"], cfg["emb_dim"])
        self.pos_emb = nn.Embedding(cfg["context_length"], cfg["emb_dim"])
        self.drop_emb = nn.Dropout(cfg["drop_rate"])

        self.trf_blocks = nn.Sequential(
            *[TransformerBlock(cfg) for _ in range(cfg["n_layers"])])

        self.final_norm = LayerNorm(cfg["emb_dim"])
        self.out_head = nn.Linear(cfg["emb_dim"], cfg["vocab_size"], bias=False)

    def forward(self, in_idx):
        batch_size, seq_len = in_idx.shape
        tok_embeds = self.tok_emb(in_idx)
        pos_embeds = self.pos_emb(torch.arange(seq_len, device=in_idx.device))
        x = tok_embeds + pos_embeds          # 相加得到总嵌入
        x = self.drop_emb(x)
        x = self.trf_blocks(x)
        x = self.final_norm(x)
        logits = self.out_head(x)            # (batch, seq_len, vocab_size)
        return logits
```

要点：
- **位置信息**：GPT-2 用可学习的位置嵌入（每个位置一个向量），与 token 嵌入相加。
- **输出头**：与 `tok_emb` 权重共享（加载官方权重时复用 `wte`），输出是词表上的 logits。

### 4.6 模型配置与参数量检查

GPT-2 small（124M）配置：

```python
GPT_CONFIG_124M = {
    "vocab_size": 50257,     # 词表大小（BPE）
    "context_length": 1024,  # 最大上下文长度
    "emb_dim": 768,          # 嵌入维度
    "n_heads": 12,           # 注意力头数
    "n_layers": 12,          # Transformer 层数
    "drop_rate": 0.1,        # Dropout 概率
    "qkv_bias": False        # Q/K/V 线性层不加偏置
}

torch.manual_seed(123)
model = GPTModel(GPT_CONFIG_124M)
model.eval()  # 推理前务必关闭 Dropout

total = sum(p.numel() for p in model.parameters())
size_mb = total * 4 / (1024 * 1024)  # float32 每参数 4 字节
print(f"Total parameters: {total:,}")        # 163,320,537
print(f"Training size: {size_mb:.2f} MB")    # 622.98 MB
# 注：输出头与 tok_emb 权重共享，去重后为 124,412,121（即"124M"的由来）
```

### 4.7 简单生成函数（权重未训练前的骨架验证）

贪心解码：每步取 logits 最大者作为下一个 token。

```python
def generate_text_simple(model, idx, max_new_tokens, context_size):
    for _ in range(max_new_tokens):
        # 超出上下文长度时裁剪，只保留最近 context_size 个 token
        idx_cond = idx[:, -context_size:]
        with torch.no_grad():
            logits = model(idx_cond)
        logits = logits[:, -1, :]  # 只看最后一个时间步
        idx_next = torch.argmax(logits, dim=-1, keepdim=True)
        idx = torch.cat((idx, idx_next), dim=1)
    return idx
```

验证（未加载权重时输出是乱码，属正常，只验证管道通畅）：

```python
start_context = "Hello, I am"
encoded = torch.tensor(tokenizer.encode(start_context)).unsqueeze(0)
out = generate_text_simple(model, encoded, max_new_tokens=10,
                           context_size=GPT_CONFIG_124M["context_length"])
print(tokenizer.decode(out.squeeze(0).tolist()))
```

✅ 完成标志：模型可前向传播，输出形状 `(batch, seq_len, 50257)`；贪心生成可运行。

---

## §5 步骤四：预训练（ch05）

**目标**：定义损失函数，实现训练循环，在小语料上完成预训练；然后加载官方 GPT-2 预训练权重，使模型真正「会说话」；最后实现带 temperature/top-k 的采样生成。

### 5.1 文本 ↔ token 辅助函数

```python
def text_to_token_ids(text, tokenizer):
    encoded = tokenizer.encode(text, allowed_special={"<|endoftext|>"})
    return torch.tensor(encoded).unsqueeze(0)  # 增加 batch 维度


def token_ids_to_text(token_ids, tokenizer):
    flat = token_ids.squeeze(0)  # 去掉 batch 维度
    return tokenizer.decode(flat.tolist())
```

### 5.2 损失函数与评估

下一个 token 预测的交叉熵：把 `(batch, seq_len, vocab_size)` 的 logits 展平成 `(batch*seq_len, vocab_size)`，与展平后的目标对比。

```python
def calc_loss_batch(input_batch, target_batch, model, device):
    input_batch, target_batch = input_batch.to(device), target_batch.to(device)
    logits = model(input_batch)
    loss = torch.nn.functional.cross_entropy(
        logits.flatten(0, 1), target_batch.flatten())
    return loss


def calc_loss_loader(data_loader, model, device, num_batches=None):
    total_loss = 0.
    if len(data_loader) == 0:
        return float("nan")
    elif num_batches is None:
        num_batches = len(data_loader)
    else:
        num_batches = min(num_batches, len(data_loader))
    for i, (input_batch, target_batch) in enumerate(data_loader):
        if i < num_batches:
            loss = calc_loss_batch(input_batch, target_batch, model, device)
            total_loss += loss.item()
        else:
            break
    return total_loss / num_batches


def evaluate_model(model, train_loader, val_loader, device, eval_iter):
    model.eval()
    with torch.no_grad():
        train_loss = calc_loss_loader(train_loader, model, device, num_batches=eval_iter)
        val_loss = calc_loss_loader(val_loader, model, device, num_batches=eval_iter)
    model.train()
    return train_loss, val_loss
```

合理性检查：随机初始化的模型初始损失应接近 `ln(50257) ≈ 10.8`。

### 5.3 训练循环 train_model_simple

```python
def generate_and_print_sample(model, tokenizer, device, start_context):
    model.eval()
    context_size = model.pos_emb.weight.shape[0]
    encoded = text_to_token_ids(start_context, tokenizer).to(device)
    with torch.no_grad():
        token_ids = generate_text_simple(
            model=model, idx=encoded,
            max_new_tokens=50, context_size=context_size)
        decoded_text = token_ids_to_text(token_ids, tokenizer)
        print(decoded_text.replace("\n", " "))
    model.train()


def train_model_simple(model, train_loader, val_loader, optimizer, device, num_epochs,
                       eval_freq, eval_iter, start_context, tokenizer):
    train_losses, val_losses, track_tokens_seen = [], [], []
    tokens_seen, global_step = 0, -1

    for epoch in range(num_epochs):
        model.train()
        for input_batch, target_batch in train_loader:
            optimizer.zero_grad()                # 清空上一批的梯度
            loss = calc_loss_batch(input_batch, target_batch, model, device)
            loss.backward()                      # 反向传播求梯度
            optimizer.step()                     # 更新权重
            tokens_seen += input_batch.numel()
            global_step += 1

            if global_step % eval_freq == 0:     # 定期评估训练/验证损失
                train_loss, val_loss = evaluate_model(
                    model, train_loader, val_loader, device, eval_iter)
                train_losses.append(train_loss)
                val_losses.append(val_loss)
                track_tokens_seen.append(tokens_seen)
                print(f"Ep {epoch+1} (Step {global_step:06d}): "
                      f"Train loss {train_loss:.3f}, Val loss {val_loss:.3f}")

        generate_and_print_sample(model, tokenizer, device, start_context)

    return train_losses, val_losses, track_tokens_seen
```

### 5.4 启动预训练（完整脚本）

训练语料用仓库自带的 `ch02/01_main-chapter-code/the-verdict.txt`（短篇小说，约 2 万 token），仅用于演示训练流程。为加速训练，上下文长度从 1024 缩短为 256。

```python
import tiktoken

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
torch.manual_seed(123)

# 训练配置（预训练时上下文缩短为 256）
GPT_CONFIG_124M["context_length"] = 256

OTHER_SETTINGS = {
    "learning_rate": 5e-4,
    "num_epochs": 10,
    "batch_size": 2,
    "weight_decay": 0.1,
}

# 读取语料并按 90/10 划分训练/验证集（按字符位置切分）
with open("the-verdict.txt", "r", encoding="utf-8") as f:
    text_data = f.read()
train_ratio = 0.90
split_idx = int(train_ratio * len(text_data))

train_loader = create_dataloader_v1(
    text_data[:split_idx],
    batch_size=OTHER_SETTINGS["batch_size"],
    max_length=GPT_CONFIG_124M["context_length"],
    stride=GPT_CONFIG_124M["context_length"],  # 无重叠窗口，避免数据泄漏到验证集
    drop_last=True, shuffle=True, num_workers=0)
val_loader = create_dataloader_v1(
    text_data[split_idx:],
    batch_size=OTHER_SETTINGS["batch_size"],
    max_length=GPT_CONFIG_124M["context_length"],
    stride=GPT_CONFIG_124M["context_length"],
    drop_last=False, shuffle=False, num_workers=0)

# 建模 + 优化器（AdamW，带权重衰减 0.1）
model = GPTModel(GPT_CONFIG_124M)
model.to(device)
optimizer = torch.optim.AdamW(
    model.parameters(),
    lr=OTHER_SETTINGS["learning_rate"],
    weight_decay=OTHER_SETTINGS["weight_decay"])

tokenizer = tiktoken.get_encoding("gpt2")
train_losses, val_losses, tokens_seen = train_model_simple(
    model, train_loader, val_loader, optimizer, device,
    num_epochs=OTHER_SETTINGS["num_epochs"], eval_freq=5, eval_iter=1,
    start_context="Every effort moves you", tokenizer=tokenizer)
```

预期：损失从约 10.8 快速下降；在小语料上若干 epoch 后验证损失会开始上升（过拟合，属预期现象）；每轮末尾的采样文本会逐渐变成有结构的英文（但基本是对训练文本的复述）。

### 5.5 保存与重载权重 / 绘制损失曲线

```python
import matplotlib.pyplot as plt

# 绘制训练/验证损失（上轴为 epoch，下轴为已见 token 数）
epochs_tensor = torch.linspace(0, OTHER_SETTINGS["num_epochs"], len(train_losses))
fig, ax1 = plt.subplots(figsize=(5, 3))
ax1.plot(epochs_tensor, train_losses, label="Training loss")
ax1.plot(epochs_tensor, val_losses, linestyle="-.", label="Validation loss")
ax1.set_xlabel("Epochs"); ax1.set_ylabel("Loss")
ax1.legend(loc="upper right")
ax2 = ax1.twiny()
ax2.plot(tokens_seen, train_losses, alpha=0)
ax2.set_xlabel("Tokens seen")
fig.tight_layout(); plt.savefig("loss-plot.pdf"); plt.show()

# 保存与重载
torch.save(model.state_dict(), "model.pth")
model = GPTModel(GPT_CONFIG_124M)
model.load_state_dict(torch.load("model.pth", weights_only=True))
model.eval()
```

### 5.6 加载官方 GPT-2 预训练权重（核心步骤）

在小语料上自训的模型只会复述训练文本。要得到真正可用的语言模型，需加载 OpenAI 发布的 GPT-2 权重（需联网，124M 版约 500MB）。

**下载与解析**（依赖 `tensorflow`，仅用于读取 `.ckpt` 格式）：

```python
import json, os
import numpy as np
import requests
from tqdm import tqdm


def download_file(url, destination, backup_url=None):
    def _attempt_download(download_url):
        response = requests.get(download_url, stream=True, timeout=60)
        response.raise_for_status()
        file_size = int(response.headers.get("Content-Length", 0))

        # 本地已有同名同大小文件则跳过（避免重复下载）
        if os.path.exists(destination):
            if file_size and file_size == os.path.getsize(destination):
                print(f"File already exists and is up-to-date: {destination}")
                return True

        block_size = 1024
        desc = os.path.basename(download_url)
        with tqdm(total=file_size, unit="iB", unit_scale=True, desc=desc) as progress_bar:
            with open(destination, "wb") as file:
                for chunk in response.iter_content(chunk_size=block_size):
                    if chunk:
                        file.write(chunk)
                        progress_bar.update(len(chunk))
        return True

    try:
        if _attempt_download(url):
            return
    except requests.exceptions.RequestException:
        if backup_url is not None:
            print(f"Primary URL ({url}) failed. Attempting backup URL: {backup_url}")
            try:
                if _attempt_download(backup_url):
                    return
            except requests.exceptions.RequestException:
                pass
        print(f"Failed to download from both primary URL ({url}) and backup URL ({backup_url}).")


def download_and_load_gpt2(model_size, models_dir):
    import tensorflow as tf

    allowed_sizes = ("124M", "355M", "774M", "1558M")
    if model_size not in allowed_sizes:
        raise ValueError(f"Model size not in {allowed_sizes}")

    model_dir = os.path.join(models_dir, model_size)
    base_url = "https://openaipublic.blob.core.windows.net/gpt-2/models"
    backup_base_url = "https://f001.backblazeb2.com/file/LLMs-from-scratch/gpt2"
    filenames = [
        "checkpoint", "encoder.json", "hparams.json",
        "model.ckpt.data-00000-of-00001", "model.ckpt.index",
        "model.ckpt.meta", "vocab.bpe"]

    os.makedirs(model_dir, exist_ok=True)
    for filename in filenames:
        download_file(
            os.path.join(base_url, model_size, filename),
            os.path.join(model_dir, filename),
            os.path.join(backup_base_url, model_size, filename))

    tf_ckpt_path = tf.train.latest_checkpoint(model_dir)
    settings = json.load(open(os.path.join(model_dir, "hparams.json"),
                              "r", encoding="utf-8"))
    params = load_gpt2_params_from_tf_ckpt(tf_ckpt_path, settings)
    return settings, params


def load_gpt2_params_from_tf_ckpt(ckpt_path, settings):
    import tensorflow as tf

    params = {"blocks": [{} for _ in range(settings["n_layer"])]}
    for name, _ in tf.train.list_variables(ckpt_path):
        variable_array = np.squeeze(tf.train.load_variable(ckpt_path, name))
        variable_name_parts = name.split("/")[1:]  # 跳过 'model/' 前缀

        target_dict = params
        if variable_name_parts[0].startswith("h"):  # 按层号 h0..h11 归入 blocks
            layer_number = int(variable_name_parts[0][1:])
            target_dict = params["blocks"][layer_number]

        for key in variable_name_parts[1:-1]:
            target_dict = target_dict.setdefault(key, {})
        target_dict[variable_name_parts[-1]] = variable_array
    return params
```

> 提示：`download_file` / `download_and_load_gpt2` 的完整可运行实现就在 `pkg/llms_from_scratch/ch05.py`，安装过本仓库包后可直接 `from llms_from_scratch.ch05 import download_and_load_gpt2`。

**把权重装进我们的 GPTModel**：注意官方权重的线性层存储为 `(in, out)` 布局，而 `nn.Linear` 使用 `(out, in)`，所以所有 `w` 矩阵都要转置；Q/K/V 存在同一个 `c_attn` 里，需按最后一维切成 3 份；输出头与 `wte` 共享。

```python
def assign(left, right):
    if left.shape != right.shape:
        raise ValueError(f"Shape mismatch. Left: {left.shape}, Right: {right.shape}")
    return torch.nn.Parameter(torch.tensor(right))


def load_weights_into_gpt(gpt, params):
    gpt.pos_emb.weight = assign(gpt.pos_emb.weight, params["wpe"])
    gpt.tok_emb.weight = assign(gpt.tok_emb.weight, params["wte"])

    for b in range(len(params["blocks"])):
        q_w, k_w, v_w = np.split(
            (params["blocks"][b]["attn"]["c_attn"])["w"], 3, axis=-1)
        gpt.trf_blocks[b].att.W_query.weight = assign(
            gpt.trf_blocks[b].att.W_query.weight, q_w.T)
        gpt.trf_blocks[b].att.W_key.weight = assign(
            gpt.trf_blocks[b].att.W_key.weight, k_w.T)
        gpt.trf_blocks[b].att.W_value.weight = assign(
            gpt.trf_blocks[b].att.W_value.weight, v_w.T)

        q_b, k_b, v_b = np.split(
            (params["blocks"][b]["attn"]["c_attn"])["b"], 3, axis=-1)
        gpt.trf_blocks[b].att.W_query.bias = assign(
            gpt.trf_blocks[b].att.W_query.bias, q_b)
        gpt.trf_blocks[b].att.W_key.bias = assign(
            gpt.trf_blocks[b].att.W_key.bias, k_b)
        gpt.trf_blocks[b].att.W_value.bias = assign(
            gpt.trf_blocks[b].att.W_value.bias, v_b)

        gpt.trf_blocks[b].att.out_proj.weight = assign(
            gpt.trf_blocks[b].att.out_proj.weight,
            params["blocks"][b]["attn"]["c_proj"]["w"].T)
        gpt.trf_blocks[b].att.out_proj.bias = assign(
            gpt.trf_blocks[b].att.out_proj.bias,
            params["blocks"][b]["attn"]["c_proj"]["b"])

        gpt.trf_blocks[b].ff.layers[0].weight = assign(
            gpt.trf_blocks[b].ff.layers[0].weight,
            params["blocks"][b]["mlp"]["c_fc"]["w"].T)
        gpt.trf_blocks[b].ff.layers[0].bias = assign(
            gpt.trf_blocks[b].ff.layers[0].bias,
            params["blocks"][b]["mlp"]["c_fc"]["b"])
        gpt.trf_blocks[b].ff.layers[2].weight = assign(
            gpt.trf_blocks[b].ff.layers[2].weight,
            params["blocks"][b]["mlp"]["c_proj"]["w"].T)
        gpt.trf_blocks[b].ff.layers[2].bias = assign(
            gpt.trf_blocks[b].ff.layers[2].bias,
            params["blocks"][b]["mlp"]["c_proj"]["b"])

        gpt.trf_blocks[b].norm1.scale = assign(
            gpt.trf_blocks[b].norm1.scale, params["blocks"][b]["ln_1"]["g"])
        gpt.trf_blocks[b].norm1.shift = assign(
            gpt.trf_blocks[b].norm1.shift, params["blocks"][b]["ln_1"]["b"])
        gpt.trf_blocks[b].norm2.scale = assign(
            gpt.trf_blocks[b].norm2.scale, params["blocks"][b]["ln_2"]["g"])
        gpt.trf_blocks[b].norm2.shift = assign(
            gpt.trf_blocks[b].norm2.shift, params["blocks"][b]["ln_2"]["b"])

    gpt.final_norm.scale = assign(gpt.final_norm.scale, params["g"])
    gpt.final_norm.shift = assign(gpt.final_norm.shift, params["b"])
    gpt.out_head.weight = assign(gpt.out_head.weight, params["wte"])
```

**执行加载**（注意使用与官方一致的完整配置，`context_length` 恢复为 1024，`qkv_bias=True` 是 GPT-2 官方权重的要求）：

```python
settings, params = download_and_load_gpt2(model_size="124M", models_dir="gpt2")

GPT_CONFIG_124M = {
    "vocab_size": 50257,
    "context_length": 1024,
    "emb_dim": settings["n_embd"],       # 768
    "n_heads": settings["n_head"],       # 12
    "n_layers": settings["n_layer"],     # 12
    "drop_rate": 0.0,                    # 推理时 Dropout 不生效，置 0 即可
    "qkv_bias": True,                    # 必须为 True 才能对上官方权重形状
}

model = GPTModel(GPT_CONFIG_124M)
model.eval()
load_weights_into_gpt(model, params)
model.to(device)
```

### 5.7 带 temperature 与 top-k 的采样生成（ch05 版 generate）

```python
def generate(model, idx, max_new_tokens, context_size, temperature=0.0, top_k=None, eos_id=None):
    for _ in range(max_new_tokens):
        idx_cond = idx[:, -context_size:]
        with torch.no_grad():
            logits = model(idx_cond)
        logits = logits[:, -1, :]

        # top-k 过滤：只保留概率最高的 k 个 token
        if top_k is not None:
            top_logits, _ = torch.topk(logits, top_k)
            min_val = top_logits[:, -1]
            logits = torch.where(logits < min_val,
                                 torch.tensor(float("-inf")).to(logits.device), logits)

        # temperature 缩放 + 采样；temperature=0 退化为贪心解码
        if temperature > 0.0:
            logits = logits / temperature
            probs = torch.softmax(logits, dim=-1)
            idx_next = torch.multinomial(probs, num_samples=1)
        else:
            idx_next = torch.argmax(logits, dim=-1, keepdim=True)

        if idx_next == eos_id:  # 遇到结束符提前停止（可选）
            break

        idx = torch.cat((idx, idx_next), dim=1)
    return idx
```

验证生成质量：

```python
token_ids = generate(
    model=model,
    idx=text_to_token_ids("Every effort moves you", tokenizer).to(device),
    max_new_tokens=25,
    context_size=GPT_CONFIG_124M["context_length"],
    top_k=50,
    temperature=1.5)
print(token_ids_to_text(token_ids, tokenizer))
# 期望输出流畅、连贯且多样的英文（temperature 1.5 + top_k 50 是书中最优组合）
```

✅ 完成标志：加载官方权重后，生成文本连贯、多样、与开头提示相关。

---

## §6 步骤五：文本分类微调（ch06）

**目标**：把加载了 GPT-2 权重的模型改造成二分类器（垃圾邮件检测）。核心改造：用最后一个 token 位置的输出作为整条文本的表征，替换输出头为 2 类，冻结大部分参数只微调「最后一个 Transformer 块 + 最终归一化层 + 新输出头」。

### 6.1 下载并构造平衡数据集（SMSSpamCollection）

```python
import zipfile, os
from pathlib import Path
import requests
import pandas as pd


def download_and_unzip_spam_data(url, zip_path, extracted_path, data_file_path):
    if data_file_path.exists():
        print(f"{data_file_path} already exists. Skipping download and extraction.")
        return
    response = requests.get(url, stream=True, timeout=60)
    response.raise_for_status()
    with open(zip_path, "wb") as out_file:
        for chunk in response.iter_content(chunk_size=8192):
            if chunk:
                out_file.write(chunk)
    with zipfile.ZipFile(zip_path, "r") as zip_ref:
        zip_ref.extractall(extracted_path)
    # 原始文件无扩展名，重命名为 .tsv 便于 pandas 读取
    os.rename(Path(extracted_path) / "SMSSpamCollection", data_file_path)
    print(f"File downloaded and saved as {data_file_path}")


def create_balanced_dataset(df):
    num_spam = df[df["Label"] == "spam"].shape[0]
    ham_subset = df[df["Label"] == "ham"].sample(num_spam, random_state=123)
    return pd.concat([ham_subset, df[df["Label"] == "spam"]])


def random_split(df, train_frac, validation_frac):
    df = df.sample(frac=1, random_state=123).reset_index(drop=True)
    train_end = int(len(df) * train_frac)
    validation_end = train_end + int(len(df) * validation_frac)
    return df[:train_end], df[train_end:validation_end], df[validation_end:]


url = "https://archive.ics.uci.edu/static/public/228/sms+spam+collection.zip"
zip_path = "sms_spam_collection.zip"
extracted_path = "sms_spam_collection"
data_file_path = Path(extracted_path) / "SMSSpamCollection.tsv"

download_and_unzip_spam_data(url, zip_path, extracted_path, data_file_path)
df = pd.read_csv(data_file_path, sep="\t", header=None, names=["Label", "Text"])
balanced_df = create_balanced_dataset(df)
balanced_df["Label"] = balanced_df["Label"].map({"ham": 0, "spam": 1})
train_df, validation_df, test_df = random_split(balanced_df, 0.7, 0.1)
train_df.to_csv("train.csv", index=None)
validation_df.to_csv("validation.csv", index=None)
test_df.to_csv("test.csv", index=None)
```

数据规模：平衡后约 1500 条（spam/ham 各半），70% 训练、10% 验证、20% 测试。

### 6.2 SpamDataset：定长化 + 填充（padding）

分类任务的输入是整条文本，需要统一长度：超过 `max_length` 截断，不足则用 50256（`<|endoftext|>`）填充。

```python
class SpamDataset(Dataset):
    def __init__(self, csv_file, tokenizer, max_length=None, pad_token_id=50256):
        self.data = pd.read_csv(csv_file)
        self.encoded_texts = [tokenizer.encode(text) for text in self.data["Text"]]

        if max_length is None:
            self.max_length = self._longest_encoded_length()
        else:
            self.max_length = max_length
            self.encoded_texts = [
                encoded_text[:self.max_length]
                for encoded_text in self.encoded_texts]

        # 右侧填充到统一长度（实践中为对齐训练集长度，可手动指定 max_length）
        self.encoded_texts = [
            encoded_text + [pad_token_id] * (self.max_length - len(encoded_text))
            for encoded_text in self.encoded_texts]

    def __getitem__(self, index):
        encoded = self.encoded_texts[index]
        label = self.data.iloc[index]["Label"]
        return (torch.tensor(encoded, dtype=torch.long),
                torch.tensor(label, dtype=torch.long))

    def __len__(self):
        return len(self.data)

    def _longest_encoded_length(self):
        return max(len(t) for t in self.encoded_texts)
```

构造三个 DataLoader（注意验证/测试集必须使用**训练集**的 `max_length`，避免不一致）：

```python
tokenizer = tiktoken.get_encoding("gpt2")

train_dataset = SpamDataset("train.csv", max_length=None, tokenizer=tokenizer)
val_dataset = SpamDataset("validation.csv", max_length=train_dataset.max_length, tokenizer=tokenizer)
test_dataset = SpamDataset("test.csv", max_length=train_dataset.max_length, tokenizer=tokenizer)

def create_dataset_loader(dataset, batch_size=8, shuffle_flag=False):
    return DataLoader(dataset, batch_size=batch_size, shuffle=shuffle_flag)

num_workers = 0
train_loader = create_dataset_loader(train_dataset, batch_size=8, shuffle_flag=True)
val_loader = create_dataset_loader(val_dataset, batch_size=8)
test_loader = create_dataset_loader(test_dataset, batch_size=8)
```

### 6.3 模型改造：冻结参数 + 替换输出头 + 解冻尾部层

```python
torch.manual_seed(123)

model = GPTModel(GPT_CONFIG_124M)  # 使用 §5.6 加载好 GPT-2 权重的配置
# 加载权重后（同 §5.6）：
# settings, params = download_and_load_gpt2(model_size="124M", models_dir="gpt2")
# load_weights_into_gpt(model, params)

# 1) 冻结全部参数（不计算梯度，省显存、防过拟合）
for param in model.parameters():
    param.requires_grad = False

# 2) 替换输出头：50257 维 → 2 类；新层默认 requires_grad=True
num_classes = 2
model.out_head = torch.nn.Linear(
    in_features=GPT_CONFIG_124M["emb_dim"], out_features=num_classes)

# 3) 解冻最后一个 Transformer 块与最终归一化层，允许它们适配任务（可选但推荐）
for param in model.trf_blocks[-1].parameters():
    param.requires_grad = True
for param in model.final_norm.parameters():
    param.requires_grad = True
```

### 6.4 分类训练函数（只取最后一个 token 的 logits）

与预训练的关键区别：损失只基于序列**最后一个位置**的输出（它聚合了全句信息）。

```python
def calc_loss_batch(input_batch, target_batch, model, device):
    input_batch, target_batch = input_batch.to(device), target_batch.to(device)
    logits = model(input_batch)[:, -1, :]  # 只取最后一个 token 位置的 logits
    loss = torch.nn.functional.cross_entropy(logits, target_batch)
    return loss


def calc_loss_loader(data_loader, model, device, num_batches=None):
    total_loss = 0.
    if len(data_loader) == 0:
        return float("nan")
    num_batches = len(data_loader) if num_batches is None else min(num_batches, len(data_loader))
    for i, (input_batch, target_batch) in enumerate(data_loader):
        if i < num_batches:
            loss = calc_loss_batch(input_batch, target_batch, model, device)
            total_loss += loss.item()
        else:
            break
    return total_loss / num_batches


def evaluate_model(model, train_loader, val_loader, device, eval_iter):
    model.eval()
    with torch.no_grad():
        train_loss = calc_loss_loader(train_loader, model, device, num_batches=eval_iter)
        val_loss = calc_loss_loader(val_loader, model, device, num_batches=eval_iter)
    model.train()
    return train_loss, val_loss


def calc_accuracy_loader(data_loader, model, device, num_batches=None):
    model.eval()
    correct_predictions, num_examples = 0, 0
    if num_batches is None:
        num_batches = len(data_loader)
    else:
        num_batches = min(num_batches, len(data_loader))
    for i, (input_batch, target_batch) in enumerate(data_loader):
        if i < num_batches:
            input_batch, target_batch = input_batch.to(device), target_batch.to(device)
            with torch.no_grad():
                logits = model(input_batch)[:, -1, :]
            predicted_labels = torch.argmax(logits, dim=-1)
            num_examples += predicted_labels.shape[0]
            correct_predictions += (predicted_labels == target_batch).sum().item()
        else:
            break
    return correct_predictions / num_examples


def train_classifier_simple(model, train_loader, val_loader, optimizer, device, num_epochs,
                            eval_freq, eval_iter):
    train_losses, val_losses, train_accs, val_accs = [], [], [], []
    examples_seen, global_step = 0, -1

    for epoch in range(num_epochs):
        model.train()
        for input_batch, target_batch in train_loader:
            optimizer.zero_grad()
            loss = calc_loss_batch(input_batch, target_batch, model, device)
            loss.backward()
            optimizer.step()
            examples_seen += input_batch.shape[0]  # 跟踪样本数（而非 token 数）
            global_step += 1

            if global_step % eval_freq == 0:
                train_loss, val_loss = evaluate_model(
                    model, train_loader, val_loader, device, eval_iter)
                train_losses.append(train_loss)
                val_losses.append(val_loss)
                print(f"Ep {epoch+1} (Step {global_step:06d}): "
                      f"Train loss {train_loss:.3f}, Val loss {val_loss:.3f}")

        train_accuracy = calc_accuracy_loader(train_loader, model, device, num_batches=eval_iter)
        val_accuracy = calc_accuracy_loader(val_loader, model, device, num_batches=eval_iter)
        print(f"Training accuracy: {train_accuracy*100:.2f}% | "
              f"Validation accuracy: {val_accuracy*100:.2f}%")
        train_accs.append(train_accuracy)
        val_accs.append(val_accuracy)

    return train_losses, val_losses, train_accs, val_accs, examples_seen
```

### 6.5 启动训练与测试集评估

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model.to(device)

torch.manual_seed(123)
optimizer = torch.optim.AdamW(model.parameters(), lr=5e-5, weight_decay=0.1)

num_epochs = 5
train_losses, val_losses, train_accs, val_accs, examples_seen = train_classifier_simple(
    model, train_loader, val_loader, optimizer, device,
    num_epochs=num_epochs, eval_freq=50, eval_iter=5)

test_accuracy = calc_accuracy_loader(test_loader, model, device)
print(f"Test accuracy: {test_accuracy*100:.2f}%")
# 预期：测试集准确率约 95%~99%（GPU 上 5 个 epoch 约几分钟）

torch.save(model.state_dict(), "review_classifier.pth")
```

要点：微调学习率（5e-5）比预训练（5e-4）低一个数量级，避免破坏预训练知识。

### 6.6 单条推理：单函数包装分类全流程（classify_review）

```python
def classify_review(text, model, tokenizer, device, max_length=None, pad_token_id=50256):
    model.eval()
    input_ids = tokenizer.encode(text)
    supported_context_length = model.pos_emb.weight.shape[0]
    input_ids = input_ids[:min(max_length, supported_context_length)]
    input_ids += [pad_token_id] * (max_length - len(input_ids))
    input_tensor = torch.tensor(input_ids, device=device).unsqueeze(0)

    with torch.no_grad():
        logits = model(input_tensor)[:, -1, :]  # 最后一个 token 位置的 logits
    predicted_label = torch.argmax(logits, dim=-1).item()
    return "spam" if predicted_label == 1 else "not spam"


text_1 = (
    "You are a winner you have been specially selected to receive \xa31000 cash "
    "or a \xa32000 award."
)
print(classify_review(text_1, model, tokenizer, device,
                      max_length=train_dataset.max_length))  # 输出：spam
text_2 = "Hey, just wanted to know if you're still up for the trip tomorrow?"
print(classify_review(text_2, model, tokenizer, device,
                      max_length=train_dataset.max_length))  # 输出：not spam
```

✅ 完成标志：测试集准确率 ≥ 95%，且两条示例分别预测为 spam / not spam。

---

## §7 步骤六：指令微调（ch07）

**目标**：在指令数据集（Alpaca 格式，5.2 万条）上继续训练，让模型学会按指令作答。关键技术：模板化输入、批量内动态填充、用 `ignore_index=-100` 掩盖填充位的损失。

### 7.1 数据集下载与 Alpaca 模板

数据集：Alpaca-52k（instruction / input / output 三字段）。模板格式：

```
Below is an instruction that describes a task. Write a response that appropriately completes the request.

### Instruction:
<指令>

### Input:
<可选输入>

### Response:
<期望回答>
```

```python
import json
import requests


def download_and_load_file(file_path, url):
    if not os.path.exists(file_path):
        response = requests.get(url, timeout=30)
        response.raise_for_status()
        with open(file_path, "w", encoding="utf-8") as file:
            file.write(response.text)
    with open(file_path, "r", encoding="utf-8") as file:
        data = json.load(file)
    return data


def format_input(entry):
    instruction_text = (
        f"Below is an instruction that describes a task. "
        f"Write a response that appropriately completes the request."
        f"\n\n### Instruction:\n{entry['instruction']}"
    )
    input_text = f"\n\n### Input:\n{entry['input']}" if entry["input"] else ""
    return instruction_text + input_text


url = "https://raw.githubusercontent.com/tatsu-lab/stanford_alpaca/main/alpaca_data.json"
data = download_and_load_file("instruction-data.json", url)

# 划分：训练/验证/测试 = 85%/5%/10%
train_portion = int(len(data) * 0.85)
test_portion = int(len(data) * 0.1)
train_data = data[:train_portion]
test_data = data[train_portion:train_portion + test_portion]
val_data = data[train_portion + test_portion:]
```

### 7.2 InstructionDataset：完整样本 = 模板 + 回答（整体编码）

```python
class InstructionDataset(Dataset):
    def __init__(self, data, tokenizer):
        self.data = data
        self.encoded_texts = []
        for entry in data:
            instruction_plus_input = format_input(entry)
            response_text = f"\n\n### Response:\n{entry['output']}"
            full_text = instruction_plus_input + response_text
            self.encoded_texts.append(tokenizer.encode(full_text))

    def __getitem__(self, index):
        return self.encoded_texts[index]

    def __len__(self):
        return len(self.data)


tokenizer = tiktoken.get_encoding("gpt2")
train_dataset = InstructionDataset(train_data, tokenizer)
print(len(train_dataset))  # 约 44,200 条训练样本；每条是长度不等的 token ID 列表
```

### 7.3 collate 函数：填充 + 输入/目标右移 + 损失掩码（核心）

逻辑分三步：
1. 每条序列末尾追加一个 `<|endoftext|>`，再按批内最长序列填充；
2. 输入 = 填充后去掉末尾一位，目标 = 去掉开头一位（右移一位对齐）；
3. **目标中的填充位置置为 -100**（保留第一个填充位作为 `<|endoftext|>` 的训练信号），使损失计算忽略这些位置。

```python
def custom_collate_fn(
    batch,
    pad_token_id=50256,
    ignore_index=-100,
    allowed_max_length=None,
    device="cpu"
):
    batch_max_length = max(len(item)+1 for item in batch)

    inputs_lst, targets_lst = [], []
    for item in batch:
        new_item = item.copy()
        new_item += [pad_token_id]          # 追加 <|endoftext|>
        padded = new_item + [pad_token_id] * (batch_max_length - len(new_item))
        inputs = torch.tensor(padded[:-1])  # 去掉末尾一位作为输入
        targets = torch.tensor(padded[1:])  # 去掉开头一位作为目标（右移）

        # 只保留第一个填充 token 作为训练信号，其余填充位设为 -100 掩码掉
        mask = targets == pad_token_id
        indices = torch.nonzero(mask).squeeze()
        if indices.numel() > 1:
            targets[indices[1:]] = ignore_index

        # 可选：截断到允许的最大长度（防止超出上下文）
        if allowed_max_length is not None:
            inputs = inputs[:allowed_max_length]
            targets = targets[:allowed_max_length]

        inputs_lst.append(inputs)
        targets_lst.append(targets)

    inputs_tensor = torch.stack(inputs_lst).to(device)
    targets_tensor = torch.stack(targets_lst).to(device)
    return inputs_tensor, targets_tensor
```

构造 DataLoader（与官方脚本一致：把 `device` 和 `allowed_max_length=1024` 预先绑进 collate 函数，防止超长样本突破上下文长度）：

```python
from functools import partial

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
customized_collate_fn = partial(custom_collate_fn, device=device, allowed_max_length=1024)

num_workers = 0
batch_size = 8

torch.manual_seed(123)
train_dataset = InstructionDataset(train_data, tokenizer)
train_loader = DataLoader(
    train_dataset, batch_size=batch_size, collate_fn=customized_collate_fn,
    shuffle=True, drop_last=True, num_workers=num_workers)

val_dataset = InstructionDataset(val_data, tokenizer)
val_loader = DataLoader(
    val_dataset, batch_size=batch_size, collate_fn=customized_collate_fn,
    shuffle=False, drop_last=False, num_workers=num_workers)

test_dataset = InstructionDataset(test_data, tokenizer)
test_loader = DataLoader(
    test_dataset, batch_size=batch_size, collate_fn=customized_collate_fn,
    shuffle=False, drop_last=False, num_workers=num_workers)
```

### 7.4 支持掩码的损失函数

```python
def calc_loss_batch(input_batch, target_batch, model, device, ignore_index=-100):
    logits = model(input_batch)
    loss = torch.nn.functional.cross_entropy(
        logits.flatten(0, 1), target_batch.flatten(), ignore_index=ignore_index)
    return loss
```

`cross_entropy` 会自动跳过目标为 -100 的位置，即填充位不参与损失。
训练/评估循环直接复用 §5.3 的 `train_model_simple`（内部的 `calc_loss_batch` 换成上面这个带 `ignore_index` 的版本）。

### 7.5 启动指令微调训练（完整脚本骨架）

```python
torch.manual_seed(123)

# 基础模型：加载 GPT-2 权重（同 §5.6）
model = GPTModel(GPT_CONFIG_124M)  # 完整 1024 上下文配置，qkv_bias=True
model.eval()
# settings, params = download_and_load_gpt2(model_size="124M", models_dir="gpt2")
# load_weights_into_gpt(model, params)
model.to(device)

optimizer = torch.optim.AdamW(model.parameters(), lr=0.00005, weight_decay=0.1)
num_epochs = 2

torch.manual_seed(123)
train_losses, val_losses, tokens_seen = train_model_simple(
    model, train_loader, val_loader, optimizer, device,
    num_epochs=num_epochs, eval_freq=5, eval_iter=5,
    start_context=format_input(val_data[0]), tokenizer=tokenizer)

# 保存微调后的模型
torch.save(model.state_dict(), "gpt2-medium-124M-instruction-finetuned.pth")
```

预期：训练损失从约 1.1 降到约 0.6；验证损失同步下降；样本生成从复述风格变为直接作答。GPU 上约 1–2 小时，CPU 上要很久。

### 7.6 推理：模型能否按指令作答

```python
i = 0
input_text = format_input(test_data[i])
token_ids = generate(
    model=model,
    idx=text_to_token_ids(input_text, tokenizer).to(device),
    max_new_tokens=256,
    context_size=model.pos_emb.weight.shape[0],
    top_k=50, temperature=1.5)
print(token_ids_to_text(token_ids, tokenizer))
# 期望：紧接 ### Response: 给出与指令相符的完整回答（而非模板复述）
```

### 7.7 评估：LLM-as-judge（可选，需本地 Ollama + llama3）

用一个更强的 LLM 当裁判给回答打分（0–100）：

```python
import psutil


def check_if_running(process_name):
    for proc in psutil.process_iter(["name"]):
        if process_name in proc.info["name"]:
            return True
    return False


def query_model(prompt, model="llama3", url="http://localhost:11434/api/chat"):
    data = {
        "model": model,
        "messages": [{"role": "user", "content": prompt}],
        "options": {  # 固定这些参数保证可复现的确定性回答
            "seed": 123,
            "temperature": 0,
            "num_ctx": 2048},
    }
    with requests.post(url, json=data, stream=True, timeout=30) as r:
        r.raise_for_status()
        response_data = ""
        for line in r.iter_lines(decode_unicode=True):
            if not line:
                continue
            response_json = json.loads(line)
            if "message" in response_json:
                response_data += response_json["message"]["content"]
    return response_data


def generate_model_scores(json_data, json_key, model="llama3"):
    scores = []
    for entry in tqdm(json_data, desc="Scoring entries"):
        prompt = (
            f"Given the input `{format_input(entry)}` "
            f"and correct output `{entry['output']}`, "
            f"score the model response `{entry[json_key]}`"
            f" on a scale from 0 to 100, where 100 is the best score. "
            f"Respond with the integer number only."
        )
        score = query_model(prompt, model)
        try:
            scores.append(int(score))
        except ValueError:
            print(f"Could not convert score: {score}")
            continue
    return scores


# 前置：安装 Ollama（https://ollama.com）并执行 `ollama pull llama3`
assert check_if_running("ollama"), "Ollama 未运行，请先执行: ollama serve"

# 先生成测试集回答并存入 JSON（对 test_data 每条调用 §7.6 的 generate，
# 结果写入 entry["model1_response"]），再：
scores = generate_model_scores(test_data, "model1_response")
print(f"Number of scores: {len(scores)}")
print(f"Average score: {sum(scores)/len(scores):.2f}")
# 预期：微调后模型的 LLM 评分显著高于微调前的 GPT-2 基线
```

✅ 完成标志：测试集样本能按指令直接作答，且 LLM-as-judge 评分较微调前明显提升。

---

## §8 收尾：全流程检查清单与常见问题

### 8.1 全流程检查清单（按序逐项打勾）

1. ☐ Python ≥ 3.10，`pip install -r requirements.txt` 成功，`torch.cuda.is_available()` 符合预期
2. ☐ §2：tiktoken 分词可编解码还原；DataLoader 输出形状 `(batch, max_length)`，目标相对输入右移一位
3. ☐ §3：MultiHeadAttention 输出形状正确，且因果性成立（后位不影响前位）
4. ☐ §4：GPTModel 参数量 163,320,537；前向输出 `(batch, seq_len, 50257)`；贪心生成可跑通（乱码属正常）
5. ☐ §5.4：预训练损失从 ≈10.8 下降，验证损失先降后升（小语料过拟合属预期）
6. ☐ §5.6：`load_weights_into_gpt` 无形状报错；生成文本连贯（关键验收点）
7. ☐ §5.7：temperature + top-k 采样生成多样且连贯的文本；模型可存为 `.pth` 并成功重载
8. ☐ §6：分类器测试集准确率 ≥ 95%；`classify_review` 对 spam/正常文本判断正确；`review_classifier.pth` 可保存/重载
9. ☐ §7：指令微调后验证损失下降；测试集样本能按指令作答；（可选）LLM 评分提升

### 8.2 常见问题

| 问题 | 原因 | 解决 |
|---|---|---|
| 生成全是乱码/重复 | 未加载预训练权重 | 完成 §5.6 后再生成 |
| 加载权重报 `Shape mismatch` | 配置不对：`qkv_bias` 必须为 `True`，上下文 1024 | 用 §5.6 中的完整配置 |
| 输入超过 `context_length` 导致掩码报错 | 序列过长 | 截断输入，或检查 `allowed_max_length` |
| CUDA OOM | batch 过大/上下文过长 | 减小 `batch_size`、缩短 `context_length` |
| GPT-2 权重下载失败 | 网络问题 | 代码已内置 Backblaze 备源，等待自动重试；或手动下载后放入 `gpt2/124M/` |
| 训练极慢（CPU） | 无 GPU | 属正常；教学实验可用更小配置（如 `ch05/03_bonus_pretraining_on_gutenberg` 的 debug 配置）验证流程 |
| macOS MPS 行为异常 | MPS 不稳定 | 按仓库约定改用 CPU 或 CUDA |
| `tensorflow` 装不上 | 版本冲突 | 只在 §5.6 需要；可先用 `pip install tensorflow-cpu` |

### 8.3 进阶扩展（官方 bonus 材料，位置见仓库）

| 主题 | 位置 |
|---|---|
| KV Cache / 快速推理 | `ch04/03_kv-cache/` |
| GQA、MLA、滑动窗口注意力 | `ch04/04_gqa/`、`ch04/05_mla/`、`ch04/06_swa/` |
| MoE 前馈层 | `ch04/07_moe/` |
| 大语料（Gutenberg）预训练 | `ch05/03_bonus_pretraining_on_gutenberg/` |
| 学习率调度器、超参搜索 | `ch05/04_learning_rate_schedulers/`、`ch05/05_bonus_hparam_tuning/` |
| GPT 转 Llama 3 / Qwen3 / Gemma3 | `ch05/07_gpt_to_llama/`、`ch05/11_qwen3/`、`ch05/12_gemma3/` |
| LoRA 参数高效微调 | `appendix-E/` |
| DPO 偏好对齐 | `ch07/04_preference-tuning-with-dpo/` |
| 多 GPU 训练（DDP） | `appendix-A/01_main-chapter-code/DDP-script.py` |

---

*本手册基于仓库官方代码整理，代码与 `pkg/llms_from_scratch/` 及各章 `01_main-chapter-code/` 保持一致；如有出入，以仓库源码为准。*

